# Server
PORT=4000
CORS_ORIGIN=http://localhost:3000

# MySQL (Windows setup.bat 기본값)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=react_structure
DB_SYNC=true

# JWT
JWT_SECRET=dev-change-me-react-structure
JWT_EXPIRES_IN=7d

# Seed admin (first boot only)
SEED_ADMIN_USERNAME=rjsgud
SEED_ADMIN_PASSWORD=rjsgud123
SEED_ADMIN_DISPLAY_NAME=rjsgud
