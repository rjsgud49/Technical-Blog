# Windows 서버 세팅 가이드

`setup.bat` 이 **자동으로 처리하는 것**과, **직접 바꿔야 하는 것**을 정리합니다.

---

## 빠른 시작

1. 프로젝트를 서버에 복사
2. `setup.bat` 더블클릭 (또는 관리자 권한 cmd에서 실행)
3. 끝나면 `start-dev.bat` 또는 세팅 마지막 `Y` 선택
4. 브라우저: http://localhost:3000  
   로그인: `backend/.env`의 `SEED_ADMIN_*` (서버 검증, 소스에 비밀번호 없음)

| 파일 | 역할 |
|------|------|
| `setup.bat` | Node/MySQL 확인, DB 생성, `.env` 생성, `npm install`, 빌드 |
| `start-dev.bat` | API + Web 개발 서버 실행 |
| `start-prod.bat` | 빌드 후 프로덕션 실행 |
| `scripts/init-db.sql` | DB `react_structure` 생성 |
| `세팅.md` | 이 문서 (수동 변경 항목) |

---

## setup.bat 이 자동으로 하는 일

- Node.js 없으면 `winget`으로 LTS 설치 시도
- `mysql.exe` 탐색 (`PATH`, MySQL 8.x, XAMPP, Laragon)
- MySQL Windows 서비스 기동 시도 (`MySQL80` 등)
- DB 접속: 비밀번호 `password` → 실패 시 **빈 비밀번호** 재시도
- `react_structure` DB 생성 (utf8mb4)
- `backend/.env`, 루트 `.env.local` 없으면 생성
- 프론트/백엔드 `npm install`
- 가능하면 production build

---

## 기본값 (자동 적용)

| 항목 | 기본값 |
|------|--------|
| MySQL 호스트 | `localhost` |
| MySQL 포트 | `3306` |
| MySQL 유저 | `root` |
| MySQL 비밀번호 | `password` (안 되면 빈 값) |
| DB 이름 | `react_structure` |
| API 포트 | `4000` |
| Web 포트 | `3000` |
| 관리자 | `setup.bat` 실행 시 입력 → `backend/.env`에만 저장 |
| JWT | `setup.bat`이 랜덤 생성 (32바이트 hex) |
| 프론트 모드 | `NEXT_PUBLIC_DATA_SOURCE=api` |

---

## 직접 세팅해야 하는 경우

아래는 **서버 환경마다 다르므로** 파일을 열어 수정하세요.

### 1) MySQL root 비밀번호가 `password`도 아니고 빈 값도 아닐 때

`backend/.env` 수정:

```env
DB_PASSWORD=여기실제비번
```

그다음:

```bat
mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS react_structure CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
```

또는 `scripts\init-db.sql` 을 수동 실행.

### 2) 외부에서 접속할 때 (공인 IP / 도메인)

`backend/.env`:

```env
CORS_ORIGIN=http://서버IP:3000,https://도메인
```

`.env.local`:

```env
NEXT_PUBLIC_API_URL=http://서버IP:4000/api
```

방화벽에서 **3000**, **4000**, (필요 시) **3306** 인바운드 허용.

### 3) JWT / 관리자 비밀번호 (운영 필수)

`backend/.env`:

```env
JWT_SECRET=충분히-긴-랜덤-문자열
SEED_ADMIN_USERNAME=you@example.com
SEED_ADMIN_PASSWORD=강한비밀번호
```

> 시드는 **유저가 없을 때만** 생성됩니다. 이미 만들어진 계정 비밀번호는 DB에서 바꾸거나 유저 삭제 후 API 재시작.

### 4) MySQL이 다른 PC에 있을 때

```env
DB_HOST=DB서버주소
DB_PORT=3306
DB_USER=...
DB_PASSWORD=...
```

### 5) Node가 설치됐는데 `setup.bat`이 못 찾을 때

- cmd/PowerShell을 **새로 연 뒤** 다시 실행  
- 또는 Node 설치 경로를 PATH에 추가  
  예: `C:\Program Files\nodejs\`

### 6) mysql.exe를 못 찾을 때

다음 중 하나:

- MySQL 설치 후 `...\MySQL Server 8.x\bin` 을 PATH에 추가
- XAMPP/Laragon 사용 중이면 해당 `mysql\bin` PATH 추가
- MySQL Workbench에서 `scripts\init-db.sql` 수동 실행 후 setup의 DB 단계는 무시해도 됨

### 7) 포트가 이미 사용 중일 때

| 파일 | 변수 |
|------|------|
| `backend/.env` | `PORT=4000` |
| `.env.local` | `NEXT_PUBLIC_API_URL=http://localhost:새포트/api` |
| Next | `npm run dev -- -p 3001` 등 |

### 8) 콘텐츠만 localStorage (인증은 항상 API)

글 데이터는 `NEXT_PUBLIC_DATA_SOURCE=local`로 localStorage를 쓸 수 있지만, **로그인은 Nest API 서버 검증만** 사용합니다. 관리자 계정은 `backend/.env`의 `SEED_ADMIN_*`로만 시드됩니다.
### 9) 운영에서 TypeORM synchronize 끄기

개발용 기본은 `DB_SYNC=true` (테이블 자동 생성).

운영:

```env
DB_SYNC=false
```

이후에는 마이그레이션으로 스키마 관리하는 것을 권장합니다.

### 10) Windows 방화벽 (외부 공개 시)

관리자 PowerShell 예시:

```powershell
New-NetFirewallRule -DisplayName "RS-Web-3000" -Direction Inbound -Protocol TCP -LocalPort 3000 -Action Allow
New-NetFirewallRule -DisplayName "RS-API-4000" -Direction Inbound -Protocol TCP -LocalPort 4000 -Action Allow
```

### 11) 서비스처럼 항상 켜두기

작업 스케줄러 / NSSM / pm2 등으로 `start-prod.bat` 또는 아래를 등록:

```bat
cd backend && npm run start:prod
cd .. && npm run start
```

---

## 파일별 수정 체크리스트

| 파일 | 언제 수정하나 |
|------|----------------|
| `backend/.env` | DB 비번, CORS, JWT, 시드 계정, 포트 |
| `.env.local` | API URL, `api`/`local` 전환 |
| `setup.bat` 상단 변수 | 앞으로 setup을 다시 돌릴 때 기본값 자체 변경 |
| Windows 방화벽 | 외부 접속 |
| MySQL 서비스 | PC 재부팅 후 자동 시작 여부 |

---

## 문제 해결

| 증상 | 확인 |
|------|------|
| API 기동 직후 종료 | MySQL 접속·DB 이름·비번 (`backend/.env`) |
| 로그인 실패 | 시드 계정 / API가 떠 있는지 / `DATA_SOURCE=api` |
| CORS 에러 | `CORS_ORIGIN`에 프론트 주소 포함 |
| 프론트가 예전 데이터만 | `.env.local`이 `local`인지 확인 → `api`로 |
| `EADDRINUSE` | 포트 충돌 → 위 7번 |

헬스 체크:

```text
http://localhost:4000/api/health
```

정상이면 `{ "ok": true, "service": "react-structure-api" }` 형태가 나옵니다.
